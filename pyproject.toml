[project]
name = "modelmesh"
version = "0.1.0"
description = "Hierarchical multi-model orchestration across Claude Code, Codex CLI, Gemini, and Grok subscriptions"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
modelmesh = "modelmesh.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
